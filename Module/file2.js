// Import Modules
const { name, sayHi } = require('./file1.js'); // Object De-structuring in JS

// Arrangements of item can be any thing but the name of the imports item will be exactly same as of exports items

sayHi(name);
console.log(module);